<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Simple Picture Database' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <% if (typeof styles !== 'undefined') { %>
        <% styles.forEach(function(style) { %>
            <link rel="stylesheet" href="<%= style %>">
        <% }); %>
    <% } %>
</head>
<body>
    <header>
        <div class="container">
            <h1>Simple Picture Database</h1>
            <nav>
                <ul>
                    <li><a href="/" aria-label="Home" tabindex="0">Home</a></li>
                    <li><a href="/upload" aria-label="Upload Pictures" tabindex="0">Upload</a></li>
                </ul>
            </nav>
        </div>
    </header>
    
    <main class="container <%= typeof mainClass !== 'undefined' ? mainClass : '' %>">
